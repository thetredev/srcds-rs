[package]
name = "srcds"
version = "0.1.0"
edition = "2021"

[lib]
name = "srcds"
path = "src/lib.rs"
crate-type = ["cdylib", "rlib"]

[dependencies]
cxx = "1.0"

[build-dependencies]
cxx-build = "1.0"
